import styled from "styled-components";

export const CartIconContainer = styled.div`
	position: relative;
	width: 45px;
	height: 45px;
	cursor: pointer;
`

export const CartIconSVG = styled.img`
	width:100%;
`

export const CartIconCount = styled.span`
	position: absolute;
	top: 40%;
	left: 50%;
	transform: translateX(-50%);

`

